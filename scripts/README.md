# Scripts

This directory is reserved for build and validation scripts.

Planned scripts:

- `build-kernel.sh`
- `extract-firmware.sh`
- `build-fedora-packages.sh`
- `build-ubuntu-packages.sh`
- `make-fedora-image.sh`
- `make-ubuntu-image.sh`
- `qemu-smoke.sh`
- `collect-hardware-logs.sh`

Scripts should be added only after the corresponding manual process is clear in
the docs.
